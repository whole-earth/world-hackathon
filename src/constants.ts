// Client/server shared constants

// LocalStorage key used for UX-only cached nullifier
export const LOCAL_WORLD_NULLIFIER_KEY = 'worldcoin_nullifier'

// Cookie name (duplicated in server lib for server-only use)
export const WORLD_NULLIFIER_COOKIE = 'w_nh'

